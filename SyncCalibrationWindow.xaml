<ui:FluentWindow x:Class="KhurramAudioRoute.SyncCalibrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Sync calibration (L2)"
        Width="468" MinWidth="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True">
    <Grid Margin="20,16,20,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="13" TextWrapping="Wrap" Foreground="#B8C4D0"
                   Text="Pause other audio. You will hear three clicks: reference, this output, reference. Say whether the middle click sounded earlier, in sync, or later compared to the outer pair. Each answer nudges the Sync slider by ±20 ms (see docs/LATENCY_PLAN.md)."/>

        <TextBlock Grid.Row="1" x:Name="HeaderText" Margin="0,16,0,0" FontWeight="SemiBold" FontSize="17" Foreground="White"
                   Text="Preparing…"/>

        <TextBlock Grid.Row="2" x:Name="StepText" Margin="0,6,0,0" FontSize="12" Foreground="#8B95A2" Text=""/>

        <TextBlock Grid.Row="3" x:Name="PlaybackHint" Margin="0,18,0,0" FontWeight="SemiBold" FontSize="13" Foreground="#74D1FF"
                   Text=""/>

        <StackPanel Grid.Row="4" Margin="0,22,0,0" Orientation="Horizontal" HorizontalAlignment="Center">
            <ui:Button x:Name="EarlierBtn" Appearance="Secondary" Content="▼ Earlier" Margin="0,0,8,0" MinWidth="120" Click="OnEarlierClick" IsEnabled="False"/>
            <ui:Button x:Name="SyncBtn" Appearance="Primary" Content="✓ In sync" Margin="0,0,8,0" MinWidth="120" Click="OnSyncClick" IsEnabled="False"/>
            <ui:Button x:Name="LaterBtn" Appearance="Secondary" Content="▲ Later" MinWidth="120" Click="OnLaterClick" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
